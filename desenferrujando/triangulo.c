#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main () {
    int n, ladoa, ladob,ladoc, sum = 0, sum_positiv =0;
    scanf("%d", &n);
    for(int i=0; i < n; i++){
        scf("%d %d %d", &ladoa,&ladob,&ladoc);

        //verificar tipo do triangulo
        sum_positiv = abs(sum);
        if(sum_positiv )
        sum = 0;
    }
    return 0;
}
